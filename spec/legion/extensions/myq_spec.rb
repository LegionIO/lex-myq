RSpec.describe Legion::Extensions::Myq do
  it "has a version number" do
    expect(Legion::Extensions::Myq::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
